/**
 * @file
 * Handles AJAX fetching of views, including filter submission and response.
 */

(function ($, Drupal, drupalSettings) {
  'use strict';

  Drupal.behaviors.tm_theme_admin = {
    attach: function (context, settings) {
    }
  };

})(jQuery, Drupal, drupalSettings);
